diff -uprN "a/hw/acpi/aml-build.c" "b/hw/acpi/aml-build.c"
--- "a/hw/acpi/aml-build.c"	2026-06-05 14:37:06.099241010 +0800
+++ "b/hw/acpi/aml-build.c"	2026-06-05 14:37:06.100248185 +0800
@@ -1153,6 +1153,28 @@ Aml *aml_device(const char *name_format,
     return var;
 }
 
+Aml *aml_power_resource(const char *resource_name, uint8_t system_level,
+							uint16_t resource_order)
+{
+	Aml *var = aml_bundle(0x84  /* PowerResOp */, AML_EXT_PACKAGE);
+
+	build_append_namestring(var->buf, "%s", resource_name);
+	build_append_byte(var->buf, system_level);
+	build_append_byte(var->buf, resource_order & 0xFF);
+	build_append_byte(var->buf, resource_order >> 8);
+
+	return var;
+}
+
+Aml *aml_thermal_zone(const char *tz_name)
+{
+	Aml *var = aml_bundle(0x85 /* ThermalZoneOp */, AML_EXT_PACKAGE);
+
+	build_append_namestring(var->buf, "%s", tz_name) ;
+
+	return var;
+}
+
 /* ACPI 1.0b: 6.4.1 ASL Macros for Resource Descriptors */
 Aml *aml_resource_template(void)
 {
@@ -2273,7 +2295,7 @@ void build_fadt(GArray *tbl, BIOSLinker
     /* ACPI1.0: INT_MODEL, ACPI2.0+: Reserved */
     build_append_int_noprefix(tbl, f->int_model /* Multiple APIC */, 1);
     /* Preferred_PM_Profile */
-    build_append_int_noprefix(tbl, 0 /* Unspecified */, 1);
+    build_append_int_noprefix(tbl, 1 /* Desktop */, 1);
     build_append_int_noprefix(tbl, f->sci_int, 2); /* SCI_INT */
     build_append_int_noprefix(tbl, f->smi_cmd, 4); /* SMI_CMD */
     build_append_int_noprefix(tbl, f->acpi_enable_cmd, 1); /* ACPI_ENABLE */
diff -uprN "a/hw/i386/acpi-build.c" "b/hw/i386/acpi-build.c"
--- "a/hw/i386/acpi-build.c"	2026-06-05 14:37:06.095819023 +0800
+++ "b/hw/i386/acpi-build.c"	2026-06-05 14:37:06.096924323 +0800
@@ -846,6 +846,59 @@ static void build_acpi0017(Aml *table)
     aml_append(table, scope);
 }
 
+static void build_fan_device(Aml *table)
+{
+	Aml *sb_scope = aml_scope("\\_SB");
+	
+	Aml *power_res = aml_power_resource("PR01", 0, 0);
+	Aml *method = aml_method("_STA", 0, AML_SERIALIZED);
+	aml_append(method, aml_return(aml_int(0x0F)));
+	aml_append(power_res, method);
+
+	method=aml_method("_ON", 0, AML_SERIALIZED);
+	aml_append(power_res, method);
+	method=aml_method("_OFF", 0, AML_SERIALIZED);
+	aml_append(power_res, method);
+	
+	aml_append(sb_scope, power_res);
+	
+	Aml *dev = aml_device("FAN0");
+	aml_append(dev, aml_name_decl("_HID", aml_eisaid("PNP0C0B")));
+	aml_append(dev, aml_name_decl("_UID", aml_int(0)));
+
+	Aml *pkg = aml_package(1);
+	aml_append(pkg, aml_name("%s", "PR01"));
+	aml_append(dev, aml_name_decl("_PR0", pkg));
+
+    aml_append(sb_scope, dev);
+    aml_append(table, sb_scope);
+}
+
+static void build_thermal_zone(Aml *table)
+{
+	Aml *sb_scope = aml_scope("\\_SB");
+	Aml *tz = aml_thermal_zone("TZ00");
+
+	Aml *method = aml_method("_AC0", 0, AML_NOTSERIALIZED) ;
+	aml_append(method, aml_return(aml_int(3730)));
+	aml_append(tz, method);
+
+	Aml *pkg = aml_package(1);
+	aml_append(pkg, aml_name("%s", "\\_SB.FAN0"));
+	aml_append(tz, aml_name_decl("_AL0", pkg));
+
+	method = aml_method("_TMP", 0, AML_NOTSERIALIZED) ;
+	aml_append(method, aml_return(aml_int(3630)));
+	aml_append(tz, method) ;
+
+	method = aml_method("_CRT", 0, AML_NOTSERIALIZED) ;
+	aml_append(method, aml_return(aml_int(3730)));
+	aml_append(tz, method);
+
+	aml_append(sb_scope, tz);
+	aml_append(table, sb_scope);
+}
+
 static void
 build_dsdt(GArray *table_data, BIOSLinker *linker,
            AcpiPmInfo *pm, AcpiMiscInfo *misc,
@@ -939,6 +992,9 @@ build_dsdt(GArray *table_data, BIOSLinke
         build_q35_pci0_int(dsdt);
     }
 
+	build_fan_device(dsdt);
+	build_thermal_zone(dsdt);
+
     if (misc->has_hpet) {
         build_hpet_aml(dsdt);
     }
@@ -1630,10 +1686,10 @@ build_dmar_q35(GArray *table_data, BIOSL
  * Helpful to speedup Windows guests and ignored by others.
  */
 static void
-build_waet(GArray *table_data, BIOSLinker *linker, const char *oem_id,
+build_wsmt(GArray *table_data, BIOSLinker *linker, const char *oem_id,
            const char *oem_table_id)
 {
-    AcpiTable table = { .sig = "WAET", .rev = 1, .oem_id = oem_id,
+    AcpiTable table = { .sig = "WSMT", .rev = 1, .oem_id = oem_id,
                         .oem_table_id = oem_table_id };
 
     acpi_table_begin(&table, table_data);
@@ -2076,7 +2132,7 @@ void acpi_build(AcpiBuildTables *tables,
     }
 
     acpi_add_table(table_offsets, tables_blob);
-    build_waet(tables_blob, tables->linker, x86ms->oem_id, x86ms->oem_table_id);
+    build_wsmt(tables_blob, tables->linker, x86ms->oem_id, x86ms->oem_table_id);
 
     /* Add tables supplied by user (if any) */
     for (u = acpi_table_first(); u; u = acpi_table_next(u)) {
diff -uprN "a/hw/i386/fw_cfg.c" "b/hw/i386/fw_cfg.c"
--- "a/hw/i386/fw_cfg.c"	2026-06-05 14:37:06.112685983 +0800
+++ "b/hw/i386/fw_cfg.c"	2026-06-05 14:37:06.113929067 +0800
@@ -216,7 +216,7 @@ void fw_cfg_build_feature_control(Machin
 void fw_cfg_add_acpi_dsdt(Aml *scope, FWCfgState *fw_cfg)
 {
     uint8_t io_size;
-    Aml *dev = aml_device("FWCF");
+    Aml *dev = aml_device("FWC");
     Aml *crs = aml_resource_template();
 
     /*
diff -uprN "a/include/hw/acpi/aml-build.h" "b/include/hw/acpi/aml-build.h"
--- "a/include/hw/acpi/aml-build.h"	2026-06-05 14:37:06.102554367 +0800
+++ "b/include/hw/acpi/aml-build.h"	2026-06-05 14:37:06.103620812 +0800
@@ -388,6 +388,9 @@ Aml *aml_error_device(void);
 /* Block AML object primitives */
 Aml *aml_scope(const char *name_format, ...) G_GNUC_PRINTF(1, 2);
 Aml *aml_device(const char *name_format, ...) G_GNUC_PRINTF(1, 2);
+Aml *aml_power_resource(const char *resource_name, uint8_t system_level,
+							uint16_t resource_order);
+Aml *aml_thermal_zone(const char *tz_name);
 Aml *aml_method(const char *name, int arg_count, AmlSerializeFlag sflag);
 Aml *aml_if(Aml *predicate);
 Aml *aml_else(void);